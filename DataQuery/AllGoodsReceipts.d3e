DataQuery {
    package 'inventory.management.system'
    name 'AllGoodsReceipts'
    query `GoodsReceipt.all
    .where((row) => row.organization == inputs.organization)
    .orderBy((row) => row.receiptDate, asc: false)`
    inputs [
        {
            name 'organization'
            type Organization
            required true
        }
    ]
    enableSync true
    needCount true
}
