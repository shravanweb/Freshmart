DataQuery {
    package 'inventory.management.system'
    name 'AllStockTransfers'
    query `StockTransfer.all
    .where((row) => row.organization == inputs.organization)
    .orderBy((row) => row.transferDate, asc: false)`
    inputs [
        {
            name 'organization'
            type Organization
            required true
        }
    ]
    enableSync true
    needCount true
}
