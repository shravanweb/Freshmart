DataQuery {
    package 'inventory.management.system'
    name 'AllUnitOfMeasures'
    query `UnitOfMeasure.all
    .where((row) => row.organization == inputs.organization)
    .orderBy((row) => row.name, asc: true)`
    inputs [
        {
            name 'organization'
            type Organization
            required true
        }
    ]
    enableSync true
    needCount true
}
