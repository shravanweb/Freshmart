DataQuery {
    package 'inventory.management.system'
    name 'AllSalesReturns'
    query `SalesReturn.all
    .where((row) => row.organization == inputs.organization)
    .orderBy((row) => row.returnDate, asc: false)`
    inputs [
        {
            name 'organization'
            type Organization
            required true
        }
    ]
    enableSync true
    needCount true
}
