DataQuery {
    package 'inventory.management.system'
    name 'AllUserProfiles'
    query `UserProfile.all
    .where((row) => row.organization == inputs.organization)
    .orderBy((row) => row.displayName, asc: true)`
    inputs [
        {
            name 'organization'
            type Organization
            required true
        }
    ]
    enableSync true
    needCount true
}
