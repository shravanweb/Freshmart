DataQuery {
    package 'inventory.management.system'
    name 'AllInventoryAdjustments'
    query `InventoryAdjustment.all
    .where((row) => row.organization == inputs.organization)
    .orderBy((row) => row.adjustmentDate, asc: false)`
    inputs [
        {
            name 'organization'
            type Organization
            required true
        }
    ]
    enableSync true
    needCount true
}
