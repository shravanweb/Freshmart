export {};

declare global {
  interface FormatException {
    // message(): String;
    // source(): String;
    // offset(): number;
    //readonly FormatException([this.message = '', this.source, this.offset]);
  }
}

// export default FormatException
