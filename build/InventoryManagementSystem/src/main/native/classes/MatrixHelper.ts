import { Offset } from "./Offset";
import Matrix4 from "./Matrix4";

export abstract class MatrixHelper {
  static getMatrixData(offset: Offset, data: number): Matrix4 {
    // TODO
    return null;
  }
}
